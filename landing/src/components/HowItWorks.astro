---
import { useTranslations, getLocale, localizePath } from '../i18n/ui';

const locale = getLocale(Astro.currentLocale);
const t = useTranslations(locale);
const docsHref = localizePath('/docs', locale);
const harnessHref = localizePath('/docs/harness', locale);
const levelsHref = localizePath('/docs/levels', locale);

const plates = [
  { n: '01', h: t('m1.h'), p: t('m1.p'), tool: t('m1.tool'), when: t('m1.when'), href: `${docsHref}#path` },
  { n: '02', h: t('m2.h'), p: t('m2.p'), tool: t('m2.tool'), when: t('m2.when'), href: `${docsHref}#path` },
  { n: '03', h: t('m3.h'), p: t('m3.p'), tool: t('m3.tool'), when: t('m3.when'), href: harnessHref },
  { n: '04', h: t('m4.h'), p: t('m4.p'), tool: t('m4.tool'), when: t('m4.when'), href: docsHref },
  { n: '05', h: t('m5.h'), p: t('m5.p'), tool: t('m5.tool'), when: t('m5.when'), href: `${levelsHref}#l2` },
];

const rows = [
  { cat: t('stack.c1'), def: 'Ghostty' },
  { cat: t('stack.c2'), def: 'Claude Code' },
  { cat: t('stack.c3'), def: 'setup-harness' },
];
---

<section class="sec" id="how">
  <div class="wrap">
    <div class="sec-head reveal">
      <span class="plate-num">{t('how.num')}</span>
      <h2 class="sec-title" set:html={t('how.title')} />
      <p class="sec-lead">{t('how.lead')}</p>
    </div>

    <div class="manual">
      {
        plates.map((plate) => (
          <div class="plate reveal">
            <div class="pn">{plate.n}</div>
            <div class="pbody">
              <h3>{plate.h}</h3>
              <p>{plate.p}</p>
            </div>
            <div class="ptool">
              <a class="toolchip" href={plate.href}>{plate.tool}</a>
              <span class="when">{plate.when}</span>
            </div>
          </div>
        ))
      }
    </div>

    <p class="manual-foot reveal">
      <span>{t('how.foot')}</span>
      <b>Ghostty</b><span class="pipe">·</span><b>Claude Code</b><span class="pipe">·</span><b
        >harness</b
      ><span class="pipe">·</span><b>your folder</b>
    </p>

    <p class="manual-foot reveal">
      <a href={docsHref}>{t('how.docs')}</a>
      <span class="pipe">·</span>
      <span>{t('how.grow')}</span>
    </p>

    <!-- your stack, your call -->
    <div class="stackcard reveal">
      <div class="sc-top">
        <h3 set:html={t('stack.h')} />
        <span class="sc-tag">{t('stack.tag')}</span>
      </div>
      <p class="sc-lead">{t('stack.lead')}</p>
      <div class="stackrows">
        {
          rows.map((row) => (
            <div class="stackrow">
              <div class="cat">{row.cat}</div>
              <div class="def">{row.def}</div>
              <div class="alt">
                <span>{t('stack.or')}</span> <b>{t('stack.soon')}</b>
              </div>
            </div>
          ))
        }
      </div>
      <p class="sc-foot" set:html={t('stack.foot')} />
    </div>
  </div>
</section>
