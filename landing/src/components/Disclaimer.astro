---
import { useTranslations, getLocale } from '../i18n/ui';
const locale = getLocale(Astro.currentLocale);
const t = useTranslations(locale);
---

<p class="disclaimer muted">{t('disclaimer')}</p>

<style>
  .disclaimer {
    font-size: 0.85rem;
    margin: 0;
  }
</style>
